{
  "scripts": {
    "start": "concurrently \"yarn server\" \"yarn client\"",
    "dev": "concurrently \"yarn watch-server\" \"yarn client\"",
    "client": "cd client && yarn start",
    "server": "cd server && yarn start",
    "watch-server": "cd server && yarn watch"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^5.0.0"
  }
}
